# Example file:
# /usr/local/opt/yabai/share/yabai/examples/skhdrc

# warp window
cmd + shift - h : yabai -m window --warp west
cmd + shift - j : yabai -m window --warp south
cmd + shift - k : yabai -m window --warp north
cmd + shift - l : yabai -m window --warp east

# swap window
alt + shift - h : yabai -m window --swap west
alt + shift - j : yabai -m window --swap south
alt + shift - k : yabai -m window --swap north
alt + shift - l : yabai -m window --swap east

# send window to monitor and follow focus
cmd + alt + shift - h : yabai -m window --display prev; yabai -m display --focus prev
cmd + alt + shift - m : yabai -m window --display recent; yabai -m display --focus recent
cmd + alt + shift - l : yabai -m window --display next; yabai -m display --focus next

# send window to desktop and follow focus
shift + cmd - 1 : yabai -m window --space 1; yabai -m space --focus 1
shift + cmd - 2 : yabai -m window --space 2; yabai -m space --focus 2
shift + cmd - 3 : yabai -m window --space 3; yabai -m space --focus 3
# shift + cmd - 4 : yabai -m window --space 4; yabai -m space --focus 4
shift + cmd - 5 : yabai -m window --space 5; yabai -m space --focus 5
shift + cmd - 6 : yabai -m window --space 6; yabai -m space --focus 6

# send window to desktop
# clashes with Sublime layout command shortcuts
# cmd + alt + shift - 1 : yabai -m window --space 1
# cmd + alt + shift - 2 : yabai -m window --space 2
# cmd + alt + shift - 3 : yabai -m window --space 3
# cmd + alt + shift - 4 : yabai -m window --space 4
# cmd + alt + shift - 5 : yabai -m window --space 5
# cmd + alt + shift - 6 : yabai -m window --space 6

# fast focus desktop
ctrl - 1 : yabai -m space --focus 1
ctrl - 2 : yabai -m space --focus 2
ctrl - 3 : yabai -m space --focus 3
ctrl - 4 : yabai -m space --focus 4
ctrl - 5 : yabai -m space --focus 5
ctrl - 6 : yabai -m space --focus 6

# toggle window zoom
alt - d : yabai -m window --toggle zoom-parent
alt - f : yabai -m window --toggle zoom-fullscreen
shift + alt - f : yabai -m window --toggle native-fullscreen

# toggle window split type
alt - e : yabai -m window --toggle split

# float / unfloat window
alt - t : yabai -m window --toggle float

# change desktop layout
ctrl + alt - a : yabai -m space --layout bsp
ctrl + alt - s : yabai -m space --layout float

###############
#### MODES ####
###############
:: default

## OPTIONS ##
:: options @

# enter
hyper - o ; options

# exit
options < escape ; default

# Toggle focus_follows_mouse
# @TODO write bash function to toggle setting
options < 1 : yabai -m config focus_follows_mouse autoraise; skhd --key "escape"
options < 0 : yabai -m config focus_follows_mouse off; skhd --key "escape"

#############
## PADDING ##
#############
:: padding @

# enter
hyper - p ; padding

# exit
padding < escape ; default

# toggle padding
alt - a : yabai -m space --toggle padding
padding < t : yabai -m space --toggle padding; skhd --key "escape"

# reset default padding
# TODO monitor-size based resizing
padding < 0 : yabai -m space --padding abs:0:0:0:0; skhd --key "escape"
padding < 9 : yabai -m space --padding abs:80:80:100:100; skhd --key "escape"
padding < 8 : yabai -m space --padding abs:160:160:180:180; skhd --key "escape"
padding < 7 : yabai -m space --padding abs:280:280:360:360; skhd --key "escape"
padding < 6 : yabai -m space --padding abs:280:280:640:640; skhd --key "escape"
padding < 1 : yabai -m space --padding abs:280:280:640:640; skhd --key "escape"
